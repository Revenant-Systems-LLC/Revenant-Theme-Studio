.htaccess
css.php
rpc/
sites/site*/admin/
sites/site*/private/
sites/site*/public/admin/
sites/site*/public/setup/
sites/site*/public/theme/
textpattern/
HISTORY.txt
README.txt
bin/
obj/
**/bin/
**/obj/
desktop.ini
build-warnings.txt
launch/


# Claude Code agent scratch — worktrees, session state, etc.
# Anything Claude drops in here is ephemeral and must never be tracked
# OR picked up by the build (see DefaultItemExcludes in the csproj).
.claude/

# Visual Studio IDE state
.vs/

# Large asset source archive — the extracted files in Assets/ are the source of truth.
Assets/Assets.zip

# Publish output from `dotnet publish` and MSIX packaging.
dist/
*.msix
*.msixbundle
*.appxupload
AGENTS.md

# Release archives and Krita autosave files — never commit
*.zip
*.kra
