<Window x:Class="Revenant_Theme_Studio.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Revenant_Theme_Studio.ViewModels"
        Title="Revenant Theme Studio"
        Height="900"
        Width="1340"
        Background="#0D0D0D"
        Foreground="#FFD700"
        WindowStartupLocation="CenterScreen"
        Icon="IconEngine/icons/Logos/_Damien_ICON.ico">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="BorderBrush" Value="#FFD700"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="4,0,0,0"/>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1A1A1A"/>
            <Setter Property="Foreground" Value="#FFD700"/>
            <Setter Property="BorderBrush" Value="#FFD700"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Revenant Theme Studio" FontSize="28" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <TabControl Grid.Row="1">
            <TabItem Header="Folder Icons">
                <Grid Margin="10">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal">
                        <TextBlock Text="Icon Library:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Width="650" Text="{Binding IconFolder, UpdateSourceTrigger=PropertyChanged}"/>
                        <Button Content="Browse" Click="BrowseIconFolder_Click"/>
                    </StackPanel>

                    <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,8,0,8">
                        <TextBlock Text="Selected Folder:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBox Width="650" Text="{Binding SelectedFolder}" IsReadOnly="True"/>
                        <Button Content="Browse" Click="BrowseTargetFolder_Click"/>
                    </StackPanel>

                    <Border Grid.Row="2" Grid.Column="0" BorderBrush="#444" BorderThickness="1" Padding="8" Margin="0,0,6,0">
                        <StackPanel>
                            <TextBlock Text="Icon Library" FontWeight="Bold"/>
                            <ListBox Height="430" ItemsSource="{Binding IconList}" SelectedItem="{Binding SelectedIcon}">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Width="80" Margin="4">
                                            <Image Width="42" Height="42" Source="{Binding PreviewImage}"/>
                                            <TextBlock Text="{Binding DisplayName}" FontSize="10" TextWrapping="Wrap" TextAlignment="Center"/>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="2" Grid.Column="1" BorderBrush="#444" BorderThickness="1" Padding="8" Margin="6,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Current Icon" FontWeight="Bold"/>
                            <TextBlock Text="{Binding CurrentFolderIconReference}" TextWrapping="Wrap" Margin="0,0,0,8"/>
                            <Image Width="96" Height="96" Source="{Binding SelectedIcon.PreviewImage}" Margin="0,6,0,8"/>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Apply" Width="140" Click="ApplyFolderIcon_Click"/>
                                <Button Content="Undo / Restore" Width="180" Click="UndoLastChange_Click"/>
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>

            <TabItem Header="Wallpapers 2.0">
                <Grid Margin="12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Render Mode:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <ComboBox Width="180" ItemsSource="{Binding Wallpaper.RenderModes}" SelectedItem="{Binding Wallpaper.SelectedRenderMode}"/>
                        <TextBlock Text="Span Behavior:" VerticalAlignment="Center" Margin="20,0,8,0"/>
                        <ComboBox Width="180" ItemsSource="{Binding Wallpaper.SpanBehaviors}" SelectedItem="{Binding Wallpaper.SelectedSpanBehavior}"/>
                        <Button Content="Refresh Displays" Margin="20,0,0,0" Click="RefreshDisplays_Click"/>
                    </StackPanel>

                    <Border Grid.Row="1" Grid.Column="0" BorderBrush="#444" BorderThickness="1" Padding="8" Margin="0,0,8,0">
                        <Grid>
                            <Canvas Width="760" Height="420" Background="#141414">
                                <ItemsControl ItemsSource="{Binding Wallpaper.DisplaySlots}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <Canvas />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    <ItemsControl.ItemContainerStyle>
                                        <Style TargetType="ContentPresenter">
                                            <Setter Property="Canvas.Left" Value="{Binding X}"/>
                                            <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                                        </Style>
                                    </ItemsControl.ItemContainerStyle>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Thumb DragDelta="DisplayThumb_DragDelta" Tag="{Binding SlotId}" Width="{Binding Width}" Height="{Binding Height}">
                                                <Thumb.Template>
                                                    <ControlTemplate TargetType="Thumb">
                                                        <Border BorderThickness="2" BorderBrush="#FFD700" Background="#242424" CornerRadius="4">
                                                            <StackPanel Margin="8">
                                                                <TextBlock Text="{Binding Label}" FontWeight="SemiBold" TextWrapping="Wrap"/>
                                                                <TextBlock Text="Drag to reposition" FontSize="11" Opacity="0.8"/>
                                                            </StackPanel>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Thumb.Template>
                                            </Thumb>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Canvas>
                        </Grid>
                    </Border>

                    <Border Grid.Row="1" Grid.Column="1" BorderBrush="#444" BorderThickness="1" Padding="10">
                        <StackPanel>
                            <TextBlock Text="Assignment" FontWeight="Bold"/>
                            <ComboBox Margin="0,8,0,0" ItemsSource="{Binding Wallpaper.DisplaySlots}" SelectedItem="{Binding Wallpaper.SelectedSlot}" DisplayMemberPath="Label"/>
                            <TextBlock Margin="0,12,0,0" Text="Local Folder Source"/>
                            <StackPanel Orientation="Horizontal">
                                <TextBox Width="220" Text="{Binding Wallpaper.LocalSourcePath, UpdateSourceTrigger=PropertyChanged}"/>
                                <Button Content="Browse" Click="BrowseWallpaperSource_Click"/>
                            </StackPanel>
                            <Button Content="Assign to Slot" Margin="0,8,0,0" Click="AssignWallpaperSource_Click" Width="140"/>
                            <Button Content="Run Rotation Tick" Margin="0,8,0,0" Click="RunRotationTick_Click" Width="160"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </TabItem>
        </TabControl>

        <Border Grid.Row="2" BorderBrush="#444" BorderThickness="1" Padding="8" Margin="0,10,0,0">
            <StackPanel>
                <TextBlock Text="{Binding StatusMessage}"/>
                <TextBlock Text="{Binding Wallpaper.StatusMessage}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
